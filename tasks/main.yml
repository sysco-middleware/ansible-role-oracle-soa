---
- include: prepare-os.yml
  become: yes
  tags:
    - prepare_os

- include: prepare-installers.yml
  become: yes
  tags:
    - prepare_installers

# tasks file for oracle-soa
- include: install-soa.yml
  become: yes
  become_user: "{{ oracle_soa_os_user }}"
  tags:
    - install

- include: create-jdev-link.yml
  become: yes
  tags:
    - create_jdev_link
